name: Validate Code
on:
  pull_request:
    branches:
      - master
jobs:
  Build-Linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt-get update && sudo apt-get install -y cmake libphysfs-dev libsdl2-dev libgl1-mesa-dev libopenal-dev libvorbis-dev libmodplug-dev libalure-dev libspeex-dev libpng-dev libsqlite3-dev libmysqlclient-dev libtomcrypt-dev libtommath-dev
      - name: Build project
        run: |
          mkdir -p build
          cd build
          cmake ..
          make
      - name: Copy resources
        run: cp -R resource/* exe/
