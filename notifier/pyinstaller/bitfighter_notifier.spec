# -*- mode: python -*-
a = Analysis(['bitfighter_notifier.py'],
             pathex=['c:\\Users\\Chris\\Documents\\bf-trunk\\notifier\\pyinstaller'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='bitfighter_notifier.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='bitfighter_notifier')
