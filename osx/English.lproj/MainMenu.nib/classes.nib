{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                restartAsClient = id; 
                restartAsClientAndServer = id; 
                restartAsClientPingingLocalhost = id; 
                restartAsServer = id; 
            }; 
            CLASS = MyOpenGLView; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSOpenGLView; 
        }
    ); 
    IBVersion = 1; 
}